{
  "blocks": {
    "block": {
      "name": "Virtual Try-On (legacy)"
    },
    "vton-widget": {
      "name": "Virtual Try-On",
      "settings": {
        "anchor_selector": {
          "label": "Custom button placement (optional)",
          "info": "CSS selector (e.g. .product-form__buttons). Leave empty for automatic placement."
        },
        "app_url": {
          "label": "App URL (fallback)",
          "info": "Leave default unless support told you otherwise."
        }
      }
    }
  }
}
